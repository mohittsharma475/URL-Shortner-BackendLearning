


post / -----  return the shortID
get /:id  ---  redirect to url

get  ----/analysis/:shortid - return no of clicked 



template ENgines

EJS
PUg JS
Handlebars 
perform server side rendering


 <!-- <ol>

    <% urls.forEach(url => { %>
    <li><%=url.shortId%></li>
    <% }) %>



    <!-- ${allURLs
          .map((url) => {
            return `<li>${url.shortId}-${url.redirectUrl}-${url.visitHistory.length}</li>`;
          })
          .join("")} -->

  </ol> -->